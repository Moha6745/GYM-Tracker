# CLAUDE.md

## Workflow

- When the user requests a code change/fix: after implementing it, pushing it, and opening a PR, **merge the PR into `main` automatically** — do not wait for separate approval.
- Exception: if the user explicitly says *before* requesting the change not to merge it, then leave the PR open (draft/ready) without merging.
